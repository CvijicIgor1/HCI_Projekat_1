<Window x:Class="Sistem_za_upravljanje_sadrzajima.ArtistDataTable"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sistem_za_upravljanje_sadrzajima"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Height="680" 
        Width="1000">
    <Grid Background="#7393B3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label
        x:Name="ArtistDataLabel"
        Content="Artist data"
        Grid.ColumnSpan="2"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
        FontSize="55"
        Foreground="Beige"
        />
        <Button
        x:Name="AddButton"
        Grid.Column="0"
        Grid.Row="1"
        Height="45"
        Width="170"
        Content="Add Artist"
        FontSize="20"
        Background="AliceBlue" Click="AddButton_Click"
        />
        <Button
        x:Name="DeleteArtistButton"
        Grid.Column="0"
        Grid.Row="2"
        Height="45"
        Width="170"
        Content="Delete Artist"
        FontSize="20"
        Background="AliceBlue" Click="DeleteArtistButton_Click"
      />
        <Button
        x:Name="LogOutButton"
        Grid.Column="0"
        Grid.Row="3"
        Height="45"
        Width="170"
        Content="Log out"
        FontSize="20"
        Background="AliceBlue" Click="LogOutButton_Click" 
           />
        
        <DataGrid 
            x:Name="ArtistsDataGrid"
            Grid.Column="1"
            Grid.Row="1"
            Grid.RowSpan="3"
            Margin="10,5,10,10"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            IsReadOnly="False"
            ItemsSource="{Binding ArtistsCollection}"
            RowHeight="70">

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="FontWeight" Value="DemiBold"/>
                    <Setter Property="FontSize" Value="16"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>

                <DataGridCheckBoxColumn 
                    Header=""
                    Binding="{Binding isSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="35">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>

                <DataGridTemplateColumn Header="Artist" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock 
                                TextAlignment="Center"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="18">
                                <Hyperlink x:Name="ArtistHyperLinkNewWindow" Click="ArtistHyperLinkNewWindow_Click">
                                    <Run Text="{Binding ArtistName}" />
                                </Hyperlink>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Image" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image 
                        Source="{Binding ImageSource}"
                        Width="60"
                        Height="60"
                        Stretch="UniformToFill"
                        HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn 
                        Header="Date added"
                        Binding="{Binding DateAdded}"
                        Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
